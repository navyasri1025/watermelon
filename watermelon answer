#include <stdio.h>

int main() {
    int weight;
    scanf("%d",&weight);
    if(weight>2 && weight<=100)
    {
        if (weight%2==0 && weight!=0)
    {
        printf("YES");
    }
    else
    {
        printf("NO");
    }
    }
    else
    {
        printf("NO");
    }
    return 0;
}
